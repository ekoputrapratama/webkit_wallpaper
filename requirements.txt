# System dependencies (Debian/Ubuntu):
# sudo apt install python3-gi gir1.2-gtk-3.0 gir1.2-webkit2-4.1 \
#     gir1.2-ayatanaappindicator3-0.1
#
# Fallback (GTK-free appindicator, if the above is unavailable):
# sudo apt install gir1.2-ayatanaappindicatorglib-2.0
#
# Arch Linux: gnome-shell-extension-appindicator (GNOME) or
# libayatana-appindicator from the repos
#
# For Wayland layer-shell support (Sway, Hyprland, KDE Plasma 6, COSMIC):
# NOTE: KDE Plasma 6 supports the wlr-layer-shell protocol, but you still
# need the GTK-specific bindings (NOT layer-shell-qt, which is Qt-only).
# sudo apt install gir1.2-gtklayershell-0.1
# Arch: yay -S gtk-layer-shell
#
# For auto-pause on fullscreen:
#   - Wayland/COSMIC: no extra package (binds zcosmic_toplevel_info_v1
#     through ctypes/libwayland-client, already present on Wayland systems).
#   - X11/XWayland fallback detection (EWMH via xprop):
#     sudo apt install x11-utils
#     Arch: yay -S xorg-xprop
